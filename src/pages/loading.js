import { HashLoader } from "react-spinners";

const Loading = () => {
  return (
    <div className="h-[400px] flex items-center justify-center">
      <HashLoader color="#FFA920" />
    </div>
  );
};

export default Loading;
